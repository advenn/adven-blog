# Hugo build output (Cloudflare rebuilds this)
/public/
/resources/
.hugo_build.lock

# Editors / OS
.idea/
.DS_Store

# Wrangler local state (keep wrangler.jsonc committed; ignore only local state)
.wrangler/
